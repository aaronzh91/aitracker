## What changed

<!-- One or two sentences. For data changes: which entities/relationships/bottlenecks. -->

## Sources

<!-- Every factual change needs a source URL + publisher + date. List them here (they must also be in the record's source fields). -->

## Checklist

- [ ] `npm run validate` passes locally
- [ ] Reported figures used where they exist; any `estimated` observation has `method_md`
- [ ] Headline/committed values are not presented as realized
- [ ] For new/changed edges: both `payer_treatment_md` and `payee_treatment_md` are filled in
- [ ] Single-sourced claims are marked `confidence: low` (or `medium` with a strong outlet)
